核心目的是什么？
让ai agent 进行初步reason 标注；

核心流程是什么？

Python 程序：获取、总结、入库；


给agent的提示词是什么？

模板：
xx年xx月xx日xxx主力大幅流入，核心原因或直接导火索是什么？
1. 使用 WebSearch 搜索能力；
2. 只写最关键的一条原因，一段话、300字以内；
例子：

2025年12月1日，中兴通讯主力大幅流入，核心原因或直接导火索是什么？
1. 使用 WebSearch 搜索能力；
2. 只写最关键的一条原因，一段话、300字以内；

2025年12月1日中兴通讯A股涨停，直接导火索是字节跳动旗下豆包手机助手技术预览版正式发布，并选择中兴通讯作为首发合作厂商，市场将其解读为AI终端落地的重要里程碑。

使用什么框架？
使用Claude Agent SDK；


代码写在哪里？
D:\ysd\qlab\research\stock\reason 下面；

输入从哪里获取？
从数据库里面获取；

输出写在哪里？
回写到 `storage/stock/data.sqlite` 的 `trend_mark.reason`；不再写实验用的 `result.md`。

本次是全部跑完吗？
不是，本次是实验，先跑20条数据；（数据库里面有1000多条）


是否限制输出必须按照例子的格式输出？
注意，注意，注意；对输出限制字数在300字以内，只要一段文字，不要md格式，不要多段落；只写核心原因或直接导火索，不要复盘、不要罗列资金数据。


使用什么模型？
D:\ysd\qlab\doc\deepseek 里面有deepseek的使用说明；
密钥从项目根目录 `.env` 的 `DEEPSEEK_API_KEY` 读取，不要把明文写进仓库。



